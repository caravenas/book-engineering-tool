# Verification contract for book-engineering-tool.
# Run by ~/.agents/workflows/scripts/verify.sh from the agent hooks (Claude Code,
# Codex, agy, Pi) and before Pi's review_diff.
#
# fast runs after every edit, under the Claude Code hook's 20 s timeout: the
# whole-project typecheck took 6 s on 2026-09-17. There is no linter configured.
# full runs when an agent stops: typecheck plus the vitest suite (3 s).

[fast]

[full]
typecheck: npx tsc --noEmit
test: npm test --silent
